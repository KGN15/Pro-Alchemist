# সেটআপ গাইড (CapCut Pro — Next.js অ্যাপ)

## ০. জিপ থেকে চালু

`capcut-pro-web-bundle.zip` আনজিপ করুন (সোর্স কোড; `node_modules` ও `.next` জিপে নেই)। আনজিপ করা ফোল্ডারে `package.json` থাকবে — সেটাই অ্যাপ রুট। সেখানে:

```bash
npm install
copy .env.example .env.local
npm run dev
```

---

## ১. প্রয়োজনীয় সার্ভিস

| কাজ | সুপারিশ (ফ্রি/লো কস্ট) |
|-----|------------------------|
| ডাটাবেস | **MongoDB Atlas** ফ্রি ক্লাস্টার — কালেকশন Auto, `MONGODB_URI` কপি করুন |
| ইমেইল (OTP) | **Resend** — ফ্রি টিয়ার; অ্যাকাউন্ট খুলে API key; টেস্টে `onboarding@resend.dev` + আপনার নিজের ইমেইলে পাঠানো যায়। অন্য বিকল্প: **Brevo**, **SendGrid** ফ্রি টিয়ার (কোড আপডেট লাগতে পারে) |
| ছবি/ভিডিও ক্লাউড | **Cloudinary** ফ্রি টিয়ার — `CLOUDINARY_*` চাবি |
| হোস্টিং | **Vercel** (Next.js) বা যেকোনো Node হোস্ট |

## ২. লোকাল চালানো

```bash
cd web
copy .env.example .env.local
# .env.local ভর্তি করুন
npm install
npm run dev
```

ব্রাউজার: `http://localhost:3000`

## ৩. `.env.local` ফিল্ড

- **MONGODB_URI** — Atlas থেকে। ডাটাবেস নাম URI তে দিতে পারেন (`/capcutpro`)।
- **AUTH_SECRET** — টার্মিনালে: `openssl rand -base64 32` (বা অনুরূপ র্যান্ড স্ট্রিং)।
- **ADMIN_EMAIL / ADMIN_PASSWORD** — আপনার অ্যাডমিন লগইন; শুধু আপনার জানা রাখুন।
- **RESEND_API_KEY** — Resend ড্যাশবোর্ড থেকে।
- **EMAIL_FROM** — প্রোডাকশনে ভেরিফাইড ডোমেইন ইমেইল (যেমন `OTP@yourdomain.com`)।
- **CLOUDINARY_*** — ড্যাশবোর্ড → Settings → API Keys।
- **NEXT_PUBLIC_APP_URL** — 프로ডে আপনার ডোমেইন (যেমন `https://your-site.com`)।

## ৪. ইমেইল OTP টেস্ট (খুবই গুরুত্বপূর্ণ)

- Resend ফ্রিতে **নির্দিষ্ট `from`** দিয়ে শুধু **ভেরিফাইড ইমেইল ঠিকানায়** টেস্ট করতে পারেন।
- `onboarding@resend.dev` দিয়ে টেস্ট করলে সাধারণত **আপনার Resend অ্যাকাউন্টের ইমেইলে** যায়।
- নিজের ডোমেইন যোগ করে `EMAIL_FROM` আপডেট করুন — তখন গ্রাহকের ইমেইলে যাবে।

## ৫. ক্লাউডিনারি — ভিডিও

- ভিডিও আপলোড করে **public_id** অ্যাডমিন প্যানেলে দিন।
- প্লেব্যাক সাইন্ড URL দিয়ে হয় — র তাই শুধু লগইন + অনুমোদিত পেমেন্ট ইউজার দেখতে পারবে।
- আরও কড়া রিস্ট্রিকশনের জন্য ক্লাউডিনারিতে **Restricted / authenticated** ডেলিভারি ব্যবহার করুন (ডকুমেন্টেশন দেখুন)।

## ৬. নিরাপত্তা চেকলিস্ট

- `.env.local` কখনো গিটে কমিট করবেন না।
- প্রোডাকশনে **শক্ত ADMIN_PASSWORD** এবং HTTPS।
- Resend/Cloudinary কী **রোটেট** করার অপশন রাখুন।
- স্প্যাম ব্লক: ইতিমধ্যে হানিপট ফিল্ড + রেট লিমিট আছে; চাইলে **Cloudflare Turnstile** ফর্মে যোগ করা যায়।

## ৭. ডিপ্লয় (Vercel)

1. রিপো জুড়ে দিন।
2. প্রজেক্ট রুট `web` নির্বাচন করুন (বা রুটে ফাইল মুভ করুন)।
3. Environment Variables গুলো Vercel এ একই নামে সেট করুন।
4. `npm run build` লোকালি একবার চেক করুন।

## ৮. রুট মানে কী

| পথ | কাজ |
|-----|-----|
| `/` | ল্যান্ডিং + এক্সিট ইন্টেন্ট পপআপ |
| `/register`, `/login` | ইউজার OTP |
| `/dashboard` | ইউজার — পেমেন্ট স্ট্যাটাস অনুযায়ী ভিডিও |
| `/dashboard/payment` | বিকাশ ডিটেইলস + স্ক্রিনশট |
| `/admin/login` | অ্যাডমিন |
| `/admin` | ইউজার তালিকা, পেমেন্ট রিকোয়েস্ট, ভিডিও CRUD |

---

প্রশ্ন থাকলে `.env.example` এর সাথে তুলনা করে ফিল্ডগুলো একে একে ভর্তি করুন।
